name: Build and Release

# vX.Y.Z 形式のタグをpushすると自動的に実行されます
on:
  push:
    tags:
      - 'v*'

# GitHub Releaseを作成・アップロードするために必須(組織所有のリポジトリでは
# リポジトリ設定の「Read and write permissions」だけでは不十分な場合があるため、
# ここでも明示的に権限を宣言しておく)
permissions:
  contents: write

# 同じタグに対して実行が2つ同時に走ってしまった場合(GitHub側のWebhook配信が
# まれに同じイベントを2回送ることがある)、後から始まった方を優先し、先に
# 走っていた方は自動でキャンセルする。これで同じバージョンのReleaseが
# 2つできてしまう不具合を防ぐ。
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: リポジトリを取得
        uses: actions/checkout@v4

      - name: Node.jsをセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 依存パッケージをインストール
        run: npm install

      - name: ビルド(公開はしない。GitHub Releaseの作成・アップロードは次のステップで行う)
        run: npx electron-builder --publish never

      - name: GitHub Releaseを作成し、ビルド成果物をアップロード
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            dist/*.exe
            dist/*.exe.blockmap
            dist/latest.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
